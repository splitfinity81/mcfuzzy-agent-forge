{
  "//": [
    "Root manifest for repo-wide tooling ONLY. No runtime code lives here.",
    "Every shippable package (scripts/forge-launcher, templates/skills/*) keeps its own",
    "manifest and is installed independently -- see AGENTS.md.",
    "",
    "DO NOT add a `packageManager` field. actions/setup-node v5+ auto-enables npm caching",
    "when it sees one, then fails looking for a lockfile layout this repo does not have.",
    "See the comment beside `Install dependencies` in .github/workflows/ci.yml."
  ],
  "name": "myforge-tooling",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "biome lint .",
    "lint:fix": "biome lint --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.11"
  }
}
